Introduction
============

TARQUIN is an algorithm which attempts to measure the quantities of components of Magnetic Resonance Spectroscopy (MRS) data.

There are two versions: the GUI, for interactive use, and the command line version for batch processing.


In EM64T Linux, the SVD in LAPACK appears to be broken for me. Use MKL instead (its free!).
